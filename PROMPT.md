# پرامپت ساخت برنامه‌ی منتور تمرکز برای ویندوز

## 1. نقش و هدف کلی

تو باید یک برنامه‌ی دسکتاپ ویندوزی با نام **Focus Mentor / منتور تمرکز** طراحی و پیاده‌سازی کنی.

این برنامه برای یک **طراح گرافیک / لوگودیزاینر** ساخته می‌شود و باید مانند یک منتور کاری عمل کند؛ منتوری که محکم، منطقی، محترمانه و غیرتحقیرآمیز است.

هدف اصلی برنامه این است که کاربر را در طول روز روی **یک پروژه‌ی اصلی از پیش تعیین‌شده** نگه دارد و از انحراف او به سمت کارهای جانبیِ ظاهراً مفید اما غیرضروری جلوگیری کند.

تمرکز برنامه روی جلوگیری از حواس‌پرتی‌های ساده مثل شبکه‌های اجتماعی نیست؛ بلکه باید با نوع پیچیده‌تری از حواس‌پرتی مقابله کند:  
**کارهایی که شبیه کار واقعی هستند، اما در عمل کاربر را از پروژه‌ی اصلی دور می‌کنند.**

---

## 2. پروفایل رفتاری کاربر

برنامه باید بر اساس الگوی رفتاری زیر طراحی شود:

- کاربر معمولاً مشکل اصلی‌اش شبکه‌های اجتماعی نیست.
- مشکل اصلی او رفتن به سمت کارهای شبه‌مولد است، مثل:
  - تست ابزارهای جدید طراحی
  - ساخت نمونه‌های جانبی
  - تولید تصویر با هوش مصنوعی بدون ارتباط مستقیم با پروژه‌ی امروز
  - کار روی ایده‌های مرتبط ولی غیرضروری
  - طراحی یا تست سایت‌های جانبی
  - وب‌گردی الهام‌محور بدون محدودیت زمانی
- نقطه‌ی شکست اصلی کاربر معمولاً **شروع کار** است، نه لزوماً ادامه دادن کار.
- کاربر با دستورهای خشک، آمرانه و تحقیرآمیز خوب عمل نمی‌کند.
- کاربر با توضیح کوتاه، منطقی و دلیل‌محور بهتر همراه می‌شود.
- با وجود لحن محترمانه، برنامه باید در عمل **محکم، جدی و سخت‌قابل‌نادیده‌گرفتن** باشد.
- پروژه و هدف روز باید در ابتدای روز مشخص و قفل شود.
- تغییر پروژه در میانه‌ی روز نباید ساده، خودکار یا بی‌هزینه باشد.

---

## 3. اصول طراحی

برنامه باید بر پایه‌ی این اصول ساخته شود:

1. **تمرکز روی یک پروژه در روز**
   - هر روز کاری باید با انتخاب یک پروژه‌ی اصلی شروع شود.
   - تمام منطق برنامه باید حول همان پروژه کار کند.

2. **قفل‌کردن تصمیم ابتدای روز**
   - پروژه، هدف و مدت تقریبی کار باید در ابتدای روز ثبت شوند.
   - تغییر آن‌ها در طول روز فقط با تأیید صریح ممکن باشد.

3. **مداخله‌ی منطقی و غیرتحقیرآمیز**
   - برنامه نباید کاربر را سرزنش کند.
   - پیام‌ها باید کوتاه، دقیق، محترمانه و دلیل‌محور باشند.

4. **حریم خصوصی**
   - برنامه نباید keylogger باشد.
   - نباید محتوای صفحه یا متن‌های تایپ‌شده را ذخیره کند.
   - فقط باید عنوان پنجره، نام فرایند و زمان استفاده را پایش کند.

5. **قابل اجرا بودن**
   - برنامه باید با Python برای Windows 10/11 قابل اجرا باشد.
   - ساختار کد باید ماژولار، تمیز و قابل توسعه باشد.

---

## 4. پلتفرم و فناوری‌ها

### پلتفرم هدف

- سیستم‌عامل: Windows 10/11
- زبان برنامه‌نویسی: Python
- نوع برنامه: برنامه‌ی دسکتاپ سبک با اجرای پس‌زمینه

### کتابخانه‌های پیشنهادی

در صورت نیاز از کتابخانه‌های زیر استفاده کن:

- `pystray` برای ساخت System Tray App
- `pywin32` برای دریافت پنجره و پردازش فعال
- `tkinter` یا `customtkinter` برای رابط کاربری
- `plyer` یا `win10toast` یا `winotify` برای اعلان‌ها
- `sqlite3` یا فایل JSON برای ذخیره‌سازی محلی

---

## 5. معماری کلی برنامه

برنامه باید از اجزای زیر تشکیل شود:

### 5.1. Tray App

برنامه باید در System Tray ویندوز اجرا شود و همیشه در پس‌زمینه فعال بماند.

منوی Tray باید شامل موارد زیر باشد:

- شروع روز کاری
- پایان روز کاری
- باز کردن تنظیمات
- Pause موقت
- مشاهده گزارش امروز
- خروج از برنامه

---

### 5.2. Daily Lock-in Dialog

در شروع روز کاری یا هنگام اجرای اولیه، برنامه باید یک فرم ساده نمایش دهد و اطلاعات زیر را از کاربر بگیرد:

- نام پروژه‌ی امروز
- هدف مشخص امروز
- مدت‌زمان تقریبی کار
- ابزارهای اصلی و ضروری برای این پروژه
- فعالیت‌هایی که امروز نباید به آن‌ها منحرف شود

پس از ثبت این اطلاعات، پروژه‌ی روز باید در حالت **Locked** ذخیره شود.

---

### 5.3. Window Monitor

برنامه باید در پس‌زمینه هر چند ثانیه یک‌بار وضعیت پنجره‌ی فعال را بررسی کند.

مواردی که باید بررسی شوند:

- عنوان پنجره‌ی فعال
- نام process فعال
- مدت‌زمان حضور کاربر روی آن پنجره

فاصله‌ی بررسی پیش‌فرض:

```text
هر 5 تا 10 ثانیه
```

---

### 5.4. Intervention Engine

این بخش باید تصمیم بگیرد چه زمانی کاربر احتمالاً از پروژه‌ی اصلی منحرف شده است و باید به او هشدار داده شود.

این موتور باید بر اساس موارد زیر عمل کند:

- whitelist
- soft blacklist
- ابزارهای ناشناخته
- مدت‌زمان باقی‌ماندن روی ابزار مشکوک
- تکرار رفتار مشابه
- تعداد دفعاتی که کاربر ادعا کرده یک فعالیت مشکوک بخشی از کار اصلی است

---

### 5.5. Timer and Logger

برنامه باید بتواند زمان‌ها و رویدادها را ثبت کند:

- زمان کار اصلی
- زمان استفاده از ابزارهای مجاز
- زمان استفاده از ابزارهای مشکوک
- تعداد overlayها
- تعداد دفعات تأیید «این بخشی از کار اصلی است»
- تعداد تغییر پروژه
- مجموع زمان Pause
- توضیحات کوتاه کاربر هنگام توجیه یک فعالیت مشکوک

---

### 5.6. Settings Window

پنجره تنظیمات باید امکان تغییر موارد زیر را بدهد:

- لیست ابزارهای مجاز
- لیست ابزارهای soft blacklist
- آستانه‌ی زمانی قبل از نمایش overlay
- مدت Pause
- حداکثر تعداد تغییر پروژه در روز
- شدت مداخله
- مسیر ذخیره‌سازی گزارش‌ها

---

## 6. جریان شروع روز کاری

وقتی کاربر روز کاری را شروع می‌کند، برنامه باید این مراحل را اجرا کند:

1. نمایش فرم Daily Lock-in
2. دریافت اطلاعات پروژه
3. ذخیره‌ی پروژه به‌عنوان پروژه‌ی قفل‌شده‌ی امروز
4. شروع تایمر کاری
5. شروع پایش پنجره‌ها
6. فعال‌کردن منطق مداخله

### اطلاعات الزامی

کاربر باید حداقل این موارد را وارد کند:

- پروژه‌ی امروز
- هدف مشخص امروز
- مدت‌زمان تقریبی کار

اگر یکی از این موارد خالی باشد، برنامه نباید روز کاری را شروع کند.

---

## 7. قفل پروژه و تغییر پروژه

پروژه‌ی روز پس از ثبت باید قفل شود.

اگر کاربر بخواهد پروژه را در طول روز تغییر دهد، برنامه باید:

1. یک هشدار منطقی نمایش دهد.
2. توضیح دهد که تغییر پروژه در میانه‌ی روز معمولاً می‌تواند نوعی فرار از شروع یا ادامه‌ی کار اصلی باشد.
3. از کاربر دلیل تغییر را بپرسد.
4. تغییر را در لاگ ذخیره کند.
5. تعداد تغییرات روز را افزایش دهد.

### قانون پیشنهادی

- تغییر اول: مجاز، اما با هشدار و ثبت دلیل
- تغییر دوم: مجاز، اما با هشدار جدی‌تر
- تغییر سوم به بعد: ثبت به‌عنوان الگوی انحراف و نمایش پیام جدی‌تر

نمونه پیام:

```text
این دومین تلاش برای تغییر پروژه امروز است.
اگر مشکل اصلی سخت بودن شروع کار است، تغییر پروژه احتمالاً مسئله را حل نمی‌کند.
آیا مطمئنی این تغییر واقعاً ضروری است؟
```

---

## 8. دسته‌بندی ابزارها و فعالیت‌ها

برنامه باید وضعیت پنجره‌ی فعال را در یکی از گروه‌های زیر قرار دهد:

### 8.1. Main Work

ابزارها یا پنجره‌هایی که مستقیماً برای پروژه‌ی امروز ضروری هستند.

مثال:

- Adobe Illustrator
- Figma
- Photoshop
- فایل یا پنجره‌ای که عنوان آن شامل نام پروژه‌ی امروز است

---

### 8.2. Allowed Tool

ابزارهایی که الزاماً ابزار اصلی نیستند، اما برای امروز مجاز شده‌اند.

مثال:

- مرورگر برای جستجوی محدود
- Notion یا Obsidian برای یادداشت
- پوشه پروژه
- ابزار مدیریت فایل

---

### 8.3. Soft Blacklisted

ابزارهایی که الزاماً ممنوع نیستند، اما احتمال بالایی دارد کاربر را وارد کار شبه‌مولد یا حواس‌پرتی کنند.

مثال:

- ابزارهای تولید تصویر با هوش مصنوعی که برای پروژه امروز مجاز نشده‌اند
- سایت‌های الهام طراحی
- YouTube
- شبکه‌های اجتماعی
- سایت‌سازها
- ابزارهای تست طراحی غیرضروری
- پروژه‌های جانبی

---

### 8.4. Unknown / Unclassified

ابزارهایی که در هیچ‌کدام از لیست‌ها نیستند.

برنامه نباید فوراً با این ابزارها مثل حواس‌پرتی قطعی رفتار کند، اما اگر استفاده از آن‌ها طولانی شد، باید از کاربر دلیل بخواهد.

---

## 9. منطق whitelist و soft blacklist

### Whitelist

اگر process name یا window title با whitelist تطبیق داشت:

- زمان آن به‌عنوان زمان کار مجاز ثبت شود.
- اگر عنوان پنجره شامل نام پروژه‌ی روز بود، امتیاز آن به‌عنوان Main Work ثبت شود.

---

### Soft Blacklist

اگر process name یا window title با soft blacklist تطبیق داشت:

- برنامه باید مدت حضور روی آن را اندازه‌گیری کند.
- اگر مدت حضور از threshold عبور کرد، overlay نمایش داده شود.
- این زمان در لاگ به‌عنوان زمان مشکوک یا حواس‌پرتی ثبت شود.

---

### Unknown

اگر ابزار ناشناخته باشد:

- اگر استفاده کوتاه بود، فقط ثبت شود.
- اگر استفاده طولانی شد، overlay سبک یا پرسش کوتاه نمایش داده شود.
- کاربر بتواند آن را موقتاً برای امروز مجاز کند یا به soft blacklist اضافه کند.

---

## 10. آستانه‌های مداخله

### مقدار پیش‌فرض

```text
15 تا 20 ثانیه برای ابزارهای soft blacklist
60 تا 120 ثانیه برای ابزارهای Unknown
```

### محدوده تنظیم

کاربر باید بتواند threshold را تغییر دهد، اما در محدوده‌ی منطقی:

```text
حداقل: 10 ثانیه
حداکثر: 180 ثانیه
```

اگر کاربر threshold را بیش از حد سخت‌گیرانه یا بیش از حد سست تنظیم کرد، برنامه باید هشدار دهد.

مثال:

```text
این آستانه خیلی کوتاه است و ممکن است باعث مزاحمت زیاد شود.
پیشنهاد می‌شود مقدار بین 15 تا 60 ثانیه باشد.
```

---

## 11. Overlay و مداخله

وقتی کاربر بیش از threshold روی ابزار مشکوک بماند، برنامه باید یک overlay یا پنجره‌ی مداخله‌گر نمایش دهد.

### ویژگی‌های Overlay

- واضح و غیرقابل‌نادیده‌گرفتن باشد.
- لزوماً برنامه‌های دیگر را نبندد.
- هیچ داده‌ای را حذف نکند.
- focus را بگیرد.
- فقط با انتخاب یکی از گزینه‌های مشخص بسته شود.
- لحن آن منطقی و محترمانه باشد.

### متن نمونه Overlay

```text
این فعالیت در لیست کارهای ضروری امروز نیست.

پروژه امروز:
{project_name}

هدف امروز:
{daily_goal}

آیا این کاری که الان انجام می‌دهی واقعاً بخشی از هدف امروز است؟
```

### گزینه‌ها

#### گزینه 1: بله، بخشی از کار اصلی است

اگر کاربر این گزینه را انتخاب کرد:

- یک فیلد توضیح کوتاه نمایش داده شود.
- کاربر باید دلیل را وارد کند.
- دلیل در لاگ ذخیره شود.
- تعداد Work-Claim افزایش یابد.
- اگر این رفتار زیاد تکرار شد، هشدار جداگانه نمایش داده شود.

نمونه پیام بعد از تکرار زیاد:

```text
در مدت کوتاهی چند بار فعالیت‌های مشکوک را به‌عنوان کار اصلی تأیید کرده‌ای.
ممکن است تعریف «کار اصلی» در حال بیش از حد گسترده شدن باشد.
بهتر است فقط فعالیت‌هایی را مجاز بدانی که مستقیماً به خروجی امروز کمک می‌کنند.
```

#### گزینه 2: نه، برگردم به کار اصلی

اگر کاربر این گزینه را انتخاب کرد:

- overlay بسته شود.
- زمان آن فعالیت به‌عنوان حواس‌پرتی ثبت شود.
- برنامه یک پیام کوتاه و هدایت‌کننده نمایش دهد.

نمونه:

```text
خوب است. حالا به پروژه‌ی قفل‌شده‌ی امروز برگرد.
```

---

## 12. محدودیت نرخ مداخله

برای جلوگیری از آزار بیش از حد، برنامه باید محدودیت نرخ داشته باشد.

قوانین پیشنهادی:

- حداکثر 3 overlay در هر 30 دقیقه
- اگر در یک بازه کوتاه overlay زیاد نمایش داده شد، شدت مداخله کاهش یابد و به اعلان سبک تبدیل شود
- اگر کاربر چند بار پشت‌سرهم «بله، بخشی از کار اصلی است» را انتخاب کرد، پیام هشدار تحلیلی نمایش داده شود
- اگر overlay زیاد نمایش داده می‌شود ولی رفتار تغییر نمی‌کند، در گزارش پایان روز ذکر شود که تنظیمات نیاز به بازبینی دارند

---

## 13. Pause موقت

برنامه باید حالت Pause داشته باشد، اما این حالت نباید راه ساده‌ای برای دور زدن برنامه باشد.

### ویژگی‌ها

- Pause فقط با اقدام صریح کاربر فعال شود.
- مدت Pause مشخص باشد.
- تایمر شمارش معکوس داشته باشد.
- بعد از پایان Pause، پایش خودکار فعال شود.
- مجموع زمان Pause در گزارش ثبت شود.

### محدودیت پیشنهادی

```text
هر Pause: حداکثر 10 تا 15 دقیقه
مجموع Pause روزانه: حداکثر 20٪ زمان کاری برنامه‌ریزی‌شده
```

اگر Pause بیش از حد استفاده شود، برنامه باید در گزارش پایان روز هشدار دهد.

---

## 14. تایمر کاری

برنامه باید از دو حالت تایمر پشتیبانی کند:

### 14.1. Free Timer

در این حالت برنامه فقط زمان کل کار را ثبت می‌کند.

### 14.2. Pomodoro

در این حالت کاربر بتواند بازه‌های کاری و استراحت تعریف کند.

مقادیر پیشنهادی:

```text
25 دقیقه کار
5 دقیقه استراحت
```

برنامه باید اجازه دهد کاربر حالت تایمر را در تنظیمات انتخاب کند.

---

## 15. شاخص‌های تحلیلی و گزارش‌دهی

برنامه باید شاخص‌های زیر را محاسبه کند:

### 15.1. Focus Ratio

نسبت زمان کار اصلی به کل زمان پایش‌شده:

```text
Focus Ratio = Main Work Time / Total Tracked Time
```

---

### 15.2. Distraction Time

مجموع زمانی که روی ابزارهای soft blacklist یا فعالیت‌های ردشده صرف شده است.

---

### 15.3. Overlay Count

تعداد دفعاتی که overlay نمایش داده شده است.

---

### 15.4. Work-Claim Count

تعداد دفعاتی که کاربر گفته است:

```text
بله، بخشی از کار اصلی است
```

---

### 15.5. Project Switch Count

تعداد دفعات تغییر پروژه در طول روز.

---

### 15.6. Pause Duration

مجموع زمان Pause در طول روز.

---

## 16. قوانین هشدار تحلیلی

برنامه باید علاوه بر هشدار لحظه‌ای، هشدار تحلیلی هم داشته باشد.

### نمونه قوانین

اگر `Work-Claim Count` زیاد بود:

```text
امروز چندین بار فعالیت‌های مشکوک را به‌عنوان کار اصلی تأیید کردی.
این می‌تواند نشانه‌ی شل شدن مرز بین کار اصلی و کار جانبی باشد.
```

اگر `Focus Ratio` پایین بود:

```text
نسبت زمان کار اصلی امروز پایین بود.
پیشنهاد می‌شود فردا پروژه را کوچک‌تر و هدف روز را دقیق‌تر تعریف کنی.
```

اگر `Project Switch Count` زیاد بود:

```text
امروز چند بار پروژه را تغییر دادی.
این می‌تواند نشانه‌ی فرار از شروع یا سختی پروژه‌ی اصلی باشد.
```

اگر `Pause Duration` زیاد بود:

```text
زمان Pause امروز زیاد بود.
اگر واقعاً به استراحت نیاز داشتی، برنامه فردا باید سبک‌تر تنظیم شود.
اگر نه، Pause احتمالاً تبدیل به راه فرار شده است.
```

---

## 17. گزارش پایان روز

در پایان روز کاری یا هنگام End Workday، برنامه باید یک گزارش خلاصه نمایش دهد.

### گزارش باید شامل موارد زیر باشد:

- نام پروژه‌ی روز
- هدف روز
- زمان کل پایش‌شده
- زمان کار اصلی
- زمان ابزارهای مجاز
- زمان ابزارهای soft blacklist
- Focus Ratio
- تعداد overlayها
- تعداد Work-Claimها
- تعداد تغییر پروژه
- مجموع Pause
- ابزارهایی که بیشترین زمان را گرفته‌اند
- فعالیت‌هایی که بیشترین انحراف را ایجاد کرده‌اند
- هشدارهای تحلیلی روز

---

## 18. گزارش هفتگی

برنامه باید امکان مشاهده‌ی روند هفتگی را فراهم کند.

گزارش هفتگی باید شامل موارد زیر باشد:

- میانگین Focus Ratio
- مجموع زمان کار اصلی
- مجموع زمان حواس‌پرتی
- پرتکرارترین ابزارهای حواس‌پرت‌کن
- تعداد کل overlayها
- تعداد کل Work-Claimها
- تعداد کل تغییر پروژه
- روزهایی که بهترین تمرکز را داشته‌اند
- روزهایی که بدترین تمرکز را داشته‌اند

---

## 19. ذخیره‌سازی داده‌ها

داده‌ها باید به‌صورت محلی ذخیره شوند.

### گزینه ساده

استفاده از JSON.

### گزینه ساختاریافته‌تر

استفاده از SQLite.

### داده‌های قابل ذخیره

- تاریخ
- نام پروژه
- هدف روز
- شروع و پایان روز کاری
- رویدادهای پنجره فعال
- زمان‌بندی استفاده از ابزارها
- overlayها
- پاسخ‌های کاربر به overlay
- توضیحات کوتاه کاربر
- pauseها
- تغییر پروژه‌ها
- گزارش‌های روزانه

### داده‌هایی که نباید ذخیره شوند

- محتوای صفحه
- متن‌های تایپ‌شده
- اطلاعات ورود
- رمزها
- محتوای وب‌سایت‌ها
- اسکرین‌شات

---

## 20. حریم خصوصی

برنامه باید در رابط کاربری خود شفاف توضیح دهد که چه چیزهایی را پایش می‌کند.

متن پیشنهادی:

```text
این برنامه فقط نام برنامه فعال، عنوان پنجره و مدت‌زمان استفاده را ثبت می‌کند.
هیچ متن تایپ‌شده، محتوای صفحه، رمز، اسکرین‌شات یا داده‌ی شخصی حساس ذخیره نمی‌شود.
همه داده‌ها به‌صورت محلی روی سیستم شما باقی می‌مانند.
```

---

## 21. لحن و شخصیت منتور

تمام متن‌های برنامه باید با شخصیت زیر نوشته شوند:

- محکم
- دقیق
- محترمانه
- منطقی
- غیرتحقیرآمیز
- کوتاه
- بدون شعارزدگی
- بدون زبان خشک و آمرانه

### نباید استفاده شود

```text
این کار ممنوع است.
داری وقتت را تلف می‌کنی.
تو باز هم منحرف شدی.
```

### بهتر است استفاده شود

```text
این فعالیت مستقیماً به هدف امروز وصل نیست.
به نظر می‌رسد این کار تمرکز امروز را کم می‌کند.
اگر این فعالیت واقعاً ضروری است، دلیل کوتاهش را ثبت کن.
```

---

## 22. ساختار فایل‌های پیشنهادی

کد را به‌صورت چندفایلی و ماژولار بنویس.

ساختار پیشنهادی:

```text
focus_mentor/
│
├── main.py
├── tray_app.py
├── monitor.py
├── intervention.py
├── overlay.py
├── timer.py
├── logger.py
├── storage.py
├── settings_ui.py
├── models.py
├── config.py
├── requirements.txt
└── README_FA.md
```

### توضیح فایل‌ها

#### `main.py`
نقطه شروع برنامه.

#### `tray_app.py`
مدیریت System Tray و منوی برنامه.

#### `monitor.py`
دریافت پنجره فعال، process name و window title.

#### `intervention.py`
تصمیم‌گیری درباره اینکه چه زمانی overlay نمایش داده شود.

#### `overlay.py`
نمایش overlay یا پنجره مداخله.

#### `timer.py`
مدیریت تایمر آزاد و Pomodoro.

#### `logger.py`
ثبت رویدادها و رخدادهای رفتاری.

#### `storage.py`
ذخیره و بازیابی داده‌ها از JSON یا SQLite.

#### `settings_ui.py`
رابط تنظیمات کاربر.

#### `models.py`
تعریف مدل‌های داده.

#### `config.py`
تنظیمات پیش‌فرض.

#### `README_FA.md`
راهنمای نصب و اجرای فارسی.

---

## 23. تنظیمات پیش‌فرض پیشنهادی

برنامه باید با تنظیمات پیش‌فرض قابل استفاده باشد.

نمونه تنظیمات:

```python
DEFAULT_MONITOR_INTERVAL_SECONDS = 5
DEFAULT_SOFT_BLACKLIST_THRESHOLD_SECONDS = 20
DEFAULT_UNKNOWN_THRESHOLD_SECONDS = 90
DEFAULT_MAX_OVERLAYS_PER_30_MINUTES = 3
DEFAULT_MAX_PROJECT_SWITCHES_PER_DAY = 2
DEFAULT_PAUSE_MINUTES = 10
DEFAULT_MAX_DAILY_PAUSE_RATIO = 0.2
```

### Whitelist پیش‌فرض پیشنهادی

```text
illustrator.exe
photoshop.exe
figma.exe
notepad.exe
explorer.exe
```

### Soft Blacklist پیش‌فرض پیشنهادی

```text
youtube.com
instagram.com
twitter.com
x.com
pinterest.com
midjourney
leonardo.ai
ideogram
canva.com
webflow.com
framer.com
```

کاربر باید بتواند این لیست‌ها را ویرایش کند.

---

## 24. الزامات اجرایی خروجی

خروجی نهایی باید شامل این موارد باشد:

1. کد کامل Python
2. ساختار چندفایلی
3. فایل `requirements.txt`
4. فایل `README_FA.md`
5. راهنمای نصب روی Windows 10/11
6. راهنمای اجرای برنامه
7. توضیح نحوه ویرایش whitelist
8. توضیح نحوه ویرایش soft blacklist
9. توضیح حریم خصوصی
10. توضیح محدودیت‌های فنی

---

## 25. معیارهای پذیرش

راه‌حل نهایی باید شرایط زیر را داشته باشد:

- روی Windows 10/11 قابل اجرا باشد.
- بدون اینترنت کار کند.
- System Tray داشته باشد.
- Daily Lock-in داشته باشد.
- پروژه روز را قفل کند.
- تغییر پروژه را ثبت و محدود کند.
- پنجره فعال را پایش کند.
- whitelist و soft blacklist داشته باشد.
- overlay مداخله‌گر داشته باشد.
- لاگ محلی تولید کند.
- گزارش پایان روز تولید کند.
- گزارش هفتگی یا حداقل زیرساخت آن را داشته باشد.
- keylogger نباشد.
- محتوای صفحه را ذخیره نکند.
- داده‌ای به اینترنت ارسال نکند.
- کد ماژولار و قابل توسعه باشد.
- راهنمای فارسی داشته باشد.

---

## 26. خروجی مورد انتظار از مدل

وقتی این پرامپت را اجرا می‌کنی، باید خروجی را به شکل یک پروژه‌ی کامل ارائه کنی، نه فقط توضیح مفهومی.

خروجی باید شامل موارد زیر باشد:

1. توضیح کوتاه معماری
2. کد همه فایل‌ها
3. `requirements.txt`
4. `README_FA.md`
5. راهنمای اجرا
6. نمونه تنظیمات اولیه
7. توضیح اینکه لاگ‌ها کجا ذخیره می‌شوند
8. توضیح اینکه چطور whitelist و soft blacklist تغییر می‌کنند

اگر به‌دلیل محدودیت پاسخ نتوانستی همه کدها را در یک پیام بدهی، ابتدا ساختار پروژه و فایل‌های اصلی را بده و سپس ادامه فایل‌ها را در پیام‌های بعدی ارائه کن.
